data_dir: "data"

recompute:
  load_clean_tokenize: false
  embed: false
  merge_dislikes: false
  sentiment: false
  features: false
  train: true

classification_type: "binary"    # or "multiclass"
use_smote: true
models:
  - random_forest
  - gradient_boosting
  - xgboost
